# ADB 로그 뷰어 - 추가 계획 (미결 사항 처리 방침 / 검색·필터 설계 / 추가 기능 제안)

> `PLAN.md`(전체 계획·구조 분석·UI 설계)에 이어지는 문서. ① 미결 사항 처리 방침, ② 검색(필터) 기능 설계, ③ 추가로 고려하면 좋을 기능 제안을 다룬다. 전부 구상 단계이며 구현은 진행하지 않는다.

## 1. 미결 사항 처리 방침

`PLAN.md`에 쌓인 미결 사항 — 5절(기술 스택/실시간 감시/다국어), 6.4절(코드값 의미표, `vd_id` 사용 여부), 8.9절(테마 기본값, 워크스페이스 구조, 상세 패널 위치, 로그명 표기), 9.6절(IPsec CSV anchor 컬럼, CSV 중복 제거 규칙, 미지정 파일 방치 시 기본 동작) — 은 아래 방침으로 처리한다.

- **지금 전부 확정하지 않는다.** 코드값 매핑표나 다른 장비/CSV 샘플처럼 근거가 부족한 항목은 **더 많은 실제 데이터를 확보한 뒤 점진적으로 보강**하는 것을 기본 전략으로 삼는다. `PLAN.md` 6.4·7.4·7.5·9.6절의 "모르면 원본 그대로 보여주고, 나중에 매핑/프리셋을 채운다"는 원칙과 일관된 방향이다.
- 첫 구현 버전은 **지금까지 확보한 정보(6~9절)만으로 최선의 기본값**을 채워 진행하고, 확신이 없는 부분(예: IPsec CSV anchor 컬럼, `act`/`oper` 전체 코드표)은 "알 수 없음 → 원본 노출" 폴백으로 동작시킨다.
- 순수 취향 문제(기본 테마, 상세 패널 위치, 로그 종류 표기명 등)는 **합리적 기본값으로 먼저 구현하고, 실사용 피드백으로 조정**한다 — 초기 논의에 시간을 오래 쓰지 않기 위함.
- 즉 미결 사항은 "구현을 막는 차단 요소"가 아니라 **추후 개선 백로그**로 관리한다. 새 장비 샘플이나 CSV가 들어올 때마다 해당 섹션을 갱신해 문서를 계속 최신 상태로 유지한다.

## 2. 검색(필터) 기능 설계

`PLAN.md` 8.3절(필터 바)과 8절의 "통합검색" 항목을 구체화한 내용. 요구사항은 **로그 종류별 컬럼에 맞춘 필터링**과 **전체 데이터에서 특정 워딩을 찾는 필터링**, 두 가지를 함께 제공하는 것이다.

### 2.1 두 가지 검색 모드

| 모드 | 용도 | 동작 범위 |
|---|---|---|
| **컬럼 기반 필터** | 로그 종류별 의미 있는 필드로 좁히는 정밀 검색 (예: 세션 로그에서 "목적지 포트=443, 동작=차단") | 현재 보고 있는 로그 종류 1개 내부 |
| **통합 키워드 검색** | 특정 문자열이 어느 로그에 있는지 모를 때 종류 구분 없이 찾는 검색 (예: 특정 IP·계정명을 전체에서 찾기) | 워크스페이스에 로드된 **6종 로그 전체** |

두 모드는 동시 사용 가능하다 — 예: 세션 화면에서 컬럼 필터로 좁힌 뒤, 그 안에서 키워드로 한 번 더 검색.

### 2.2 컬럼 기반 필터 — 로그 종류별 항목 (초안)

`PLAN.md` 8.5절 컬럼 프리셋을 기준으로, 각 로그 종류 화면 상단 필터 바에 아래 항목을 기본 제공한다.

| 로그 종류 | 필터 항목 | 입력 방식 |
|---|---|---|
| 세션(session) | 기간, 출발지/목적지 IP, 출발지/목적지 포트, 프로토콜, 동작(허용/차단), 정책 ID, Zone | 기간=날짜 범위 선택기, IP·포트=텍스트(부분/정확 일치), 프로토콜·동작·Zone=드롭다운 |
| 감사(audit) | 기간, 관리자 계정, 접속 유형, 위험도, IP 주소 | 드롭다운(접속유형/위험도) + 텍스트(계정/IP) |
| 인증(uauth) | 기간, 사용자 ID, 접속 유형, 결과(성공/실패), IP 주소, 그룹 | 드롭다운/텍스트 혼합 |
| 시스템(system) | 기간, 프로세스, 위험도 | 프로세스는 종류가 많을 수 있어 자동완성 콤보박스 |
| IPsec | 기간, 출발지/목적지, 상태(msg 기반) | 텍스트/드롭다운 |
| SSL-VPN(ssl) | 기간, 로컬/원격 주소, 상태(msg 기반) | 텍스트/드롭다운 |

- 드롭다운형 필터(접속 유형, 위험도, 프로세스, 동작 등)는 **코드를 하드코딩하지 않고, 현재 불러온 데이터에 실제로 등장한 값만 동적으로 뽑아** 옵션을 구성한다 → `PLAN.md` 7.5절 "코드값 매핑은 별도 설정으로 분리, 모르는 값도 그대로 노출" 원칙과 연결되며, 다른 장비에서 새로운 값이 나와도 필터가 자동으로 대응한다.
- 여러 필터는 기본적으로 **AND 조건**으로 결합한다. 필터를 걸수록 상단에 "적용된 필터" 칩(chip)으로 표시하고, 칩을 개별적으로 지우거나 "필터 초기화"로 한 번에 해제한다.
- 필터 적용 결과 건수를 실시간으로 표시한다 (예: "3,781건 중 128건 표시").

### 2.3 통합 키워드 검색

- 화면 상단(`PLAN.md` 8.3절 목업의 "통합검색")에 항상 노출되는 검색창.
- 검색어를 입력하면 **현재 워크스페이스에 로드된 모든 로그 종류**를 대상으로, 사람이 읽는 값 기준(디코딩된 IP, 해석된 라벨, 메시지 텍스트 등)으로 일치·부분일치 검색을 수행한다.
- 검색 결과는 "통합 검색 결과" 화면(`PLAN.md` 8.4절 화면 목록 4번)에서 **로그 종류별로 그룹핑해서 건수와 함께** 보여주고, 그룹을 클릭하면 해당 로그 종류 화면으로 이동하면서 검색어가 필터·하이라이트 상태로 그대로 이어진다.
- 일치한 텍스트는 결과 목록에서 하이라이트 표시한다.
- 대소문자 구분 없음을 기본으로 하고, 필요 시 "정확히 일치" 토글을 검색창 옆에 제공한다.
- 입력 중 실시간 반영하되(디바운스 적용) `PLAN.md` 6.1절에서 확인한 기존 인덱스(`ltime`/`ip_src`/`ip_dst`/`dport` 등)와 CSV의 메모리 내 파싱 데이터를 활용해 응답 지연을 최소화한다.

### 2.4 컬럼 필터 vs 통합 검색의 관계

- 통합 검색은 "이 값이 어디 있는지 모를 때" 쓰는 넓은 그물, 컬럼 필터는 "이 종류의 로그를 정확한 조건으로 좁힐 때" 쓰는 정밀 도구 — 대체재가 아니라 보완 관계로 설계한다.
- "저장된 필터"(자주 쓰는 조합에 이름 붙여 재사용)는 4.1절 제안 기능과 연결되며, 1절 방침대로 지금은 백로그로 남겨둔다.

## 3. 이 문서의 미결 사항

- 컬럼 필터의 "포함/정확히 일치/이상·이하" 등 연산자 종류를 얼마나 세분화할지 (초기 버전은 단순 포함/일치 정도로 시작하는 게 무난해 보임).
- 통합 검색 대상에 "해석 전 원시값(raw code)"까지 포함할지, 사람이 읽는 값에서만 검색할지.

## 4. 추가로 고려하면 좋을 기능 제안

지금까지의 분석·설계를 바탕으로, 요청받지 않았지만 있으면 유용할 것으로 판단되는 기능들을 정리한다. 전부 **제안 단계**이며 우선순위·채택 여부는 추후 논의한다.

### 4.1 분석 편의 기능

- **저장된 필터/검색 프리셋**: 자주 쓰는 필터 조합(예: "최근 24시간 내 차단된 세션")에 이름을 붙여 저장하고 원클릭으로 재적용.
- **시간대별 로그량 타임라인**: 화면 상단에 작은 히스토그램으로 시간대별 로그 건수를 시각화해, 특이하게 몰린 구간(트래픽 급증, 반복 로그인 실패 등)을 한눈에 발견하게 함. 막대를 드래그해 해당 구간으로 기간 필터를 바로 적용하는 인터랙션도 고려.
- **로그 간 상관 보기(cross-log correlation)**: 세션 로그의 특정 IP나 인증 로그의 특정 사용자를 클릭하면, 비슷한 시간대에 다른 로그 종류(감사/인증/세션)에 남은 관련 기록을 모아서 보여주는 "관련 로그 보기" 패널. 비전문가가 사건을 앞뒤로 추적할 때 특히 유용.
- **행 단위 메모/태그**: 특정 로그 행에 "확인 필요", "오탐 처리" 같은 메모나 태그를 남겨두고, 나중에 태그 기준으로 다시 모아볼 수 있게 함 (로컬에만 저장, 원본 백업 파일은 건드리지 않음).
- **로그 공백/누락 구간 감지**: `ltime` 기준으로 비정상적으로 긴 공백 구간이 있으면 "이 구간은 로그가 없습니다(회전/삭제로 인한 공백일 수 있음)"라고 안내 — 데이터 누락을 오탐(버그)으로 착각하지 않도록.
- **경량 규칙 기반 하이라이트**: SIEM 수준은 아니지만, 간단한 사용자 정의 규칙(예: "동일 IP에서 로그인 실패 5회 이상")에 맞는 행을 표에서 자동으로 강조 표시.

### 4.2 탐색성 / 사용성

- **최근 워크스페이스 목록**: 최근에 연 폴더(백업 세트)를 시작 화면에 목록으로 보여줘 재방문 시 빠르게 다시 열 수 있게 함.
- **용어 설명 툴팁**: 컬럼 헤더나 상세 패널의 전문 용어(예: "NAT", "SPI", "Zone")에 마우스를 올리면 짧은 설명이 뜨는 툴팁 — 비전문가 대상이라는 목적에 부합.
- **최초 실행 온보딩 투어**: 처음 실행 시 "폴더 열기 → 사이드바에서 종류 선택 → 필터/검색" 흐름을 짧게 안내하는 가이드 오버레이.
- **OS 파일 연결(파일 더블클릭으로 열기)**: `.adb` 파일을 더블클릭하면 바로 이 앱으로 열리도록 macOS/Windows 양쪽에 파일 확장자 연결 등록.

### 4.3 협업 / 산출물 공유

- **필터링된 결과 리포트 내보내기(PDF/이미지)**: CSV 내보내기(`PLAN.md` 4절)에 더해, 비전문가 보고용으로 현재 필터 결과를 요약 리포트(제목/기간/조건/표) 형태의 PDF로 저장하는 기능.
- **두 백업(워크스페이스) 비교 보기**: 예를 들어 설정 변경 전/후 감사 로그를 나란히 놓고 비교하는 diff형 화면 — 우선순위는 낮음.

### 4.4 보안 / 신뢰성

- **완전 오프라인 동작 원칙 명시**: 방화벽 로그라는 민감한 데이터 특성상, 이 앱은 어떤 형태로도 외부 서버에 데이터를 전송하지 않고 전부 로컬에서만 처리한다는 원칙을 제품 설계 원칙 + (원한다면) 앱 내 안내 문구로 명시.
- **원본 백업 파일 불변성 보장**: `PLAN.md` 6.5절에서 다룬 읽기 전용/임시 복사 원칙을 앱 전체의 일관된 규칙으로 — 어떤 기능(메모, 필터 저장 등)도 원본 `.adb`/`.csv` 파일 자체를 수정하지 않고, 앱 자체 로컬 설정/캐시 공간에만 기록.

### 4.5 성능 / 인프라 (사용자에게 직접 보이지 않는 기반 기능)

- **로컬 인덱싱 캐시**: 여러 파일(특히 CSV)을 동시에 로드했을 때 통합 검색·필터 응답 속도를 위해, 최초 로드 시 자체 로컬 인덱스(예: 임시 SQLite)를 만들어두는 백그라운드 처리.
- **상태 표시줄**: 현재 로드된 파일 수, 총 용량, 총 행 수 등을 화면 하단에 상시 표시해 사용자가 지금 무엇을 보고 있는지 가늠하게 함.

## 5. 4절 제안 기능의 구체적 구현 방안

4절 각 항목을 실제로 어떻게 만들지에 대한 기술적 방안. `PLAN.md` 3절(Electron/Tauri 미확정)·9.5절(SQLite/CSV 공통 내부 스키마·어댑터 구조)을 전제로 하며, 프레임워크가 아직 확정되지 않은 부분은 양쪽 모두에 대한 방안을 표기한다. **여전히 설계 문서이며 코드 작성 단계는 아니다.**

### 5.1 분석 편의 기능

**저장된 필터/검색 프리셋**
- 데이터 모델: `{ id, name, logType, conditions: [{field, operator, value}], createdAt }` 형태의 JSON.
- 저장 위치: 원본 백업 폴더가 아니라 **앱 전용 로컬 데이터 폴더**(Electron: `app.getPath('userData')`, Tauri: `app_data_dir()`)에 `presets.json`으로 저장 — 4.4절 "원본 불변성" 원칙과 연결.
- UI 연동: 필터 바의 "필터 저장" 버튼 → 이름 입력 모달 → 저장 즉시 필터 바 옆 "저장된 필터 ▾" 드롭다운에 추가. 프리셋 선택 시 저장된 조건 배열을 그대로 현재 필터 상태(스토어)에 주입해 재실행.

**시간대별 로그량 타임라인**
- 집계 방식: `.adb`(SQLite)는 이미 `ltime` 인덱스가 있으므로 `SELECT (ltime / 버킷초) AS bucket, COUNT(*) FROM {table} GROUP BY bucket` 형태의 집계 쿼리로 즉시 계산. CSV는 로드 시 메모리에 올라간 배열을 한 번 순회하며 동일한 방식으로 버킷팅.
- 버킷 크기는 현재 보고 있는 기간 폭에 따라 자동 조정(예: 하루 이내→시간 단위, 한 달 이상→일 단위).
- 렌더링: 외부 차트 라이브러리 의존을 늘리기보다, **직접 그린 SVG 막대그래프**로 구현 — 번들 크기와 두 OS 간 렌더링 차이(폰트/안티앨리어싱)를 최소화하기 위함(`PLAN.md` 8.8절 "자체 CSS로 재정의" 원칙과 동일 맥락).
- 인터랙션: 막대 위 드래그로 구간 선택(`onBrushStart/Move/End`) → 선택 구간을 2.2절 "기간" 필터 값으로 바로 반영.

**로그 간 상관 보기(cross-log correlation)**
- 전제: 9.5절 "공통 내부 스키마" 덕분에 로그 종류가 달라도 `ip_src`/`ip_dst`/`uname` 같은 정규화된 필드명은 동일하게 유지됨.
- 동작: 사용자가 IP나 사용자명 셀을 클릭 → 해당 값 + 기준 시각(±N분, 기본 10분 정도로 초안 설정) 을 조건으로 **로드된 모든 로그 종류에 대해 동일한 필터 쿼리를 병렬 실행** → 종류별 건수와 함께 슬라이드 패널에 표시(2.3절 통합 검색 결과 화면과 동일한 그룹핑 UI 재사용).
- 시간 창(±N분) 값은 설정에서 조정 가능하게 열어둠.

**행 단위 메모/태그**
- 저장 위치: 앱 로컬 데이터 폴더 안에 **주석 전용 사이드카 SQLite**(예: `annotations.sqlite`), 원본 폴더에는 아무것도 만들지 않음.
- 로우 식별 문제: `.adb`/CSV 테이블 모두 별도의 고유 PK가 없으므로(6.2절 스키마 참고), **해당 행의 전체 필드값을 해시한 값**을 로우 키로 사용. 파일 식별은 `경로+크기+수정시각`을 해시해 `file_id`로 사용.
- 원본 파일이 나중에 달라지면(해시 불일치) "원본이 변경된 것 같습니다 — 기존 메모를 유지할까요?"라고 안내하고 고아 메모로 별도 보관.
- UI: 행 우클릭 → "메모/태그 추가", 태그가 붙은 행은 표에 작은 마커 표시, 사이드바 하단에 "태그: 확인 필요" 같은 필터 진입점 제공.

**로그 공백/누락 구간 감지**
- 5.1 "타임라인" 집계 결과를 재사용 — 버킷 사이 간격이 해당 로그 종류의 **중앙값 간격의 N배**(예: 3배) 이상이거나 절대 기준(예: 1시간) 이상 비면 "공백 구간"으로 표시.
- 전수 스캔이 아니라 이미 계산된 버킷 집계 결과에서 파생하므로 추가 비용이 크지 않음.
- 타임라인 위에 음영 처리 + 툴팁으로 "2026-08-07 ~ 2026-08-20 데이터 없음" 형태로 안내.

**경량 규칙 기반 하이라이트**
- 규칙 모델: `{ id, name, logType, condition: {field, operator, value}, group?: {by: field, minCount, windowMinutes}, color }`.
- 단순 조건(그룹 없음)은 2.2절 컬럼 필터와 **동일한 조건 평가 엔진을 재사용** — 결과를 "숨김" 대신 "강조 스타일 부여"로 다르게 적용만 하면 됨(엔진 이원화 방지).
- 그룹/횟수 조건(예: "동일 IP 로그인 실패 5회 이상")은 `GROUP BY field HAVING COUNT(*) >= N` 형태 쿼리로 계산. 실시간 상시 평가는 MVP 범위를 벗어나므로, 초기 버전은 **사용자가 "규칙 검사 실행"을 눌렀을 때만 계산**하는 방식으로 단순화(상시 모니터링은 이후 버전 검토).

### 5.2 탐색성 / 사용성

**최근 워크스페이스 목록**
- 앱 로컬 설정 파일에 `recentWorkspaces: [{ path, lastOpenedAt, fileCount }]` 최대 10개 유지, 열 때마다 갱신.
- 시작 화면 진입 시 각 경로 존재 여부를 확인해, 폴더가 사라졌으면 "찾을 수 없음" 표시와 함께 목록에서 제거하는 옵션 제공.

**용어 설명 툴팁**
- `PLAN.md` 6~7절에서 이미 정리된 필드 설명(NAT, SPI, Zone, 정책 ID 등)을 `glossary.json`(`{ 필드key: 설명 }`)으로 정리해 앱에 내장.
- 공통 `<용어라벨>` 컴포넌트가 헤더/상세 패널 라벨을 감싸고, 마우스 오버 또는 키보드 포커스 시 툴팁 노출(접근성 고려해 hover 전용이 아니게).
- 새로운 필드가 발견될 때마다(1절 방침처럼) 이 JSON만 갱신하면 되는 구조.

**최초 실행 온보딩 투어**
- 대형 서드파티 투어 라이브러리 대신, **타깃 요소 좌표 기반의 자체 스포트라이트 오버레이 컴포넌트**를 직접 구현 — 두 OS에서 동일하게 보이도록 렌더링을 완전히 통제하기 위함(8.8절과 동일 이유).
- 단계 정의는 정적 설정 배열(`{ targetSelector, title, description }[]`)로 관리.
- 최초 실행 여부는 로컬 설정의 `onboardingCompleted` 플래그로 판단하고, 설정 화면에 "도움말 다시 보기" 버튼으로 재실행 가능하게 함.

**OS 파일 연결**
- Electron: `electron-builder`의 `fileAssociations` 설정으로 `.adb` 확장자를 앱에 연결, macOS의 `open-file` 이벤트/Windows의 `process.argv`로 전달된 경로를 받아 워크스페이스로 오픈.
- Tauri: `tauri.conf.json`의 번들 파일 연결 설정 + 딥링크/오픈 이벤트를 Rust 쪽에서 받아 프런트엔드로 전달.
- **`.csv`는 확장자 연결에서 제외** — 사용자 PC에 이미 엑셀 등 기본 CSV 프로그램이 지정돼 있을 가능성이 높아, 이를 가로채면 오히려 불편할 수 있음. CSV는 앱 내 "가져오기" 흐름으로만 연다.

### 5.3 협업 / 산출물 공유

**필터링된 결과 리포트 내보내기(PDF)**
- 접근 방식: 커스텀 PDF 생성 엔진을 직접 번들하기보다, **현재 필터 결과를 인쇄용 HTML 뷰로 렌더링한 뒤 OS 표준 인쇄 기능을 이용해 "PDF로 저장"**하도록 유도 (Electron은 `webContents.printToPDF()`로 무대화면 인쇄, Tauri는 `window.print()` 호출 후 OS 인쇄 다이얼로그의 "PDF로 저장" 옵션 이용) — 어느 쪽이든 별도 PDF 라이브러리 없이 두 OS에서 동일한 결과를 얻을 수 있음.
- 인쇄용 뷰에는 제목/조회 기간/적용된 필터 조건 요약을 표 위에 함께 렌더링.
- 행 수가 지나치게 많으면(예: 5,000행 초과) 리포트 생성 전에 "필터를 더 좁혀주세요" 안내로 무리한 렌더링을 방지.

**두 백업(워크스페이스) 비교 보기**
- 구현 난이도가 낮은 방식으로 한정: 두 워크스페이스를 각각 정상적으로 로드한 뒤, 같은 필터 조건으로 조회한 결과를 **행 내용 해시 기준 집합 연산**(A에만 있음 / B에만 있음 / 공통)으로 비교 — 순서 기반 diff 알고리즘이 아니라 집합 비교라 구현이 단순함.
- 우선순위가 낮으므로 1절 방침에 따라 백로그 유지, 위 방식은 실제 착수 시점의 참고용 메모.

### 5.4 보안 / 신뢰성

**완전 오프라인 동작 원칙**
- Tauri 채택 시: v2의 명시적 권한(capabilities) 모델을 이용해 **네트워크 관련 권한을 아예 선언하지 않음** — 프레임워크 차원에서 강제 가능.
- Electron 채택 시: 자동 업데이트·크래시 리포팅처럼 기본적으로 외부 통신을 유발하는 모듈을 기본 비활성화하거나 완전히 제외.
- 빌드/CI 단계에 `fetch(`, `XMLHttpRequest`, `axios` 등 네트워크 호출 패턴이 코드에 남아있는지 검사하는 간단한 린트/그렙 스텝을 추가해 원칙이 지켜지는지 기계적으로 확인.

**원본 백업 파일 불변성 보장**
- `.adb`(SQLite) 연결은 항상 **읽기 전용/불변 모드**로 오픈 (SQLite URI `?mode=ro&immutable=1`, Rust `rusqlite`라면 `OpenFlags::SQLITE_OPEN_READ_ONLY`) — 6.5절에서 확인한 `-wal`/`-shm` 부산물 생성 자체를 원천 차단.
- 메모/태그(5.1)·프리셋(5.1)·인덱싱 캐시(5.5) 등 앱이 만드는 모든 파생 데이터는 예외 없이 앱 전용 로컬 데이터 폴더에만 기록.

### 5.5 성능 / 인프라

**로컬 인덱싱 캐시**
- CSV는 `.adb`와 달리 자체 인덱스가 없으므로, 최초 가져오기 시 **동일한 정규화 스키마로 임시 SQLite(앱 캐시 폴더 내 파일 또는 메모리 DB)에 적재**하고 `ltime`/IP/사용자 컬럼에 인덱스를 걸어, `.adb` 소스와 **동일한 SQL 기반 필터·검색 엔진**을 그대로 재사용할 수 있게 함 (형식별로 검색 로직을 이원화하지 않기 위한 핵심 장치).
- 캐시 무효화 기준: 원본 파일의 `경로+크기+수정시각` 해시가 바뀌었을 때만 재생성.

**상태 표시줄**
- 별도 백엔드 계산 없이, 9.4절 "가져오기 미리보기"에서 이미 산출하는 파일 수/용량/행 수 상태를 화면 하단에 상시 바인딩만 하면 되는 순수 UI 작업.

---
*다음 단계: `PLAN.md`의 남은 미결 사항 + 이 문서의 3절 미결 사항 + 4절 제안 기능 중 채택할 항목을 정리해, 1절 방침대로 "지금 확보한 정보 기준 최선의 기본값"으로 상세 요구사항을 확정하고 구현 착수.*
